#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run fast checks first
echo "🔎 Running lint..."
yarn lint || exit 1

# Ensure tests pass (non-watch)
echo "🧪 Running tests..."
yarn test --run || exit 1
