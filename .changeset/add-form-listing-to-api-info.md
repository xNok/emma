---
'@xnok/emma-form-builder': patch
---

Add form listing to /api/info endpoint in local development server. The endpoint now returns an array of available forms with their metadata (id, name, url, apiUrl, bundleUrl).
